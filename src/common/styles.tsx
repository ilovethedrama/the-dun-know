import styled from "styled-components";

export const PageContainer = styled.div`
  position:relative;
  height: 100vh;
  border:2px solid red;
    overflow-y:scroll;
    display: flex;
    flex-flow: column;

`;